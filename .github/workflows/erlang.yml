name: Erlang CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Erlang
      uses: erlef/setup-beam@v1
      with:
        otp-version: "26.0"
        
    - name: Install Rebar3
      run: |
        curl -LO https://s3.amazonaws.com/rebar3/rebar3
        chmod +x rebar3
        sudo mv rebar3 /usr/local/bin/
        
    - name: Build
      run: |
        rebar3 deps
        rebar3 compile
        
    - name: Run Tests
      run: |
        rebar3 eunit
        
    - name: Run Dialyzer
      run: |
        rebar3 dialyzer || echo "Dialyzer completed with warnings"
        
    - name: Build Docs
      if: github.ref == 'refs/heads/main'
      run: |
        rebar3 ex_doc
        
    - name: Build and generate docs
      run: |
        rebar3 compile
        rebar3 ex_doc

    - name: Package Erlang release for Hex.pm
      run: |
        # Buat paket; hasilnya ditaruh di _build/default/hex/
        rebar3 hex package
        # Cari file .tar yang baru dibuat
        TARBALL=$(ls _build/default/hex/*.tar)
        echo "Created release tarball: $TARBALL"

    - name: Publish to Hex.pm via API
      env:
        HEX_API_KEY: ${{ secrets.GitHubIoT }}
      run: |
        # Ambil kembali nama file .tar
        TARBALL=$(ls _build/default/hex/*.tar)
        echo "Uploading $TARBALL to Hex.pm…"
        curl --fail -v \
          -H "Authorization: Bearer $HEX_API_KEY" \
          -F "tarball=@${TARBALL}" \
          https://repo.hex.pm/api/packages/githubiot/releases
